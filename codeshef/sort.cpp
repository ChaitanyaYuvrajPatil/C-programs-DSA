#include <iostream>
using namespace std;
#include <String>
#include <vector>

int main()
{
    vector<int> v1;
    int temp, temp2, min, loc;
    v1 = {1, 5, 3, 6, 2};
    for (int i = 0; i < v1.size() - 1; i++)
    {
        temp = v1[i];

        for (int j = i + 1; j < v1.size(); j++)
        {
            min = v1[j];
            loc = j;
            if (min <)
        }
    }
}